class GUIStyle {
    constructor(params) {
        Object.assign(this, params);
    }
}
// params: text_size; text_color; container_size; container_color; centered_x; centered_y; anchor_x; anchor_y; button_size; button_color; text_padding; slider_size; slider_color; handle_size; handle_color; handle_padding; checkbox_size; checkbox_color; checkbox_inner_size; checkbox_inner_color;, checkbox_inner_padding; image_scale_x; image_scale_y; button_sprite; slider_sprite; handle_sprite


//handle padding is a Vector2, but only the x axis is used :)